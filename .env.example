# 复制为 .env 并填入你的 Coding Plan API Key（Anthropic 协议）
#
# 注意：当前 loadConfig 只从环境变量读取 *密钥*（ANTHROPIC_AUTH_TOKEN / CODEPLAN_API_KEY）。
# 下面的 BASE_URL / MODEL 不会被读取（spec「不做（首期）：环境变量覆盖全部字段」）。
#
# 想切换模型：在 TUI 里用 `/model <id>` 动态切换（立即对下一个任务生效、无需重启），
#   或写默认模型到 .ai-code-cli/config.json（见 config.example.json）。改这里的 MODEL 没用。
#
# 当前可用的模型组（/model 无参可查看，/model <id> 切换）：
#   moonshot/kimi-k2.5、deepseek/deepseek-v4-pro、xiaomi/mimo-v2.5-pro、ali/qwen3.7-max、
#   deepseek/deepseek-v4-flash、google/gemini-3.5-flash、zhipu/glm-5、zhipu/glm-5.2
ANTHROPIC_AUTH_TOKEN=your_api_key_here

# 以下两行仅作记录，不生效（baseURL/model 实际取自 DEFAULTS 或 config.json）：
ANTHROPIC_BASE_URL=https://ai-kas.kso.net/codeplan/anthropic
# ANTHROPIC_MODEL=deepseek/deepseek-v4-pro
